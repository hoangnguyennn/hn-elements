export type RateProps = {
  /** Chỉ ra rằng rate có chức năng clear hay không. */
  clearable?: boolean
  /** Nhãn aria cho rate. */
  ariaLabel?: string
  /** Nội dung gợi ý cho rate. */
  hint?: string
}
